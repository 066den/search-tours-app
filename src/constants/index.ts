// Константы приложения

// API endpoints
export const API_ENDPOINTS = {
  // Будет добавлено после получения api.js
} as const

// Роуты приложения
export const ROUTES = {
  HOME: '/',
  TOUR_DETAILS: '/tour/:id',
} as const

// Другие константы
export const DEFAULT_SEARCH_PARAMS = {
  destination: '',
  departureCity: '',
} as const

